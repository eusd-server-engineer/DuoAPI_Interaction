# Duo API Credentials - EXAMPLE
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control!

# Get these from Duo Admin Panel > Applications > Admin API
DUO_IKEY=DIXXXXXXXXXXXXXXXXXX
DUO_SKEY=YYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYY
DUO_HOST=api-XXXXXXXX.duosecurity.com

# Optional settings
# RATE_LIMIT_MS=800
# LOG_DIR=logs
# BACKUP_DIR=backups

# Email notification settings (optional)
# For EUSD internal mail server (no authentication required):
SMTP_SERVER=mail.eusd.org
SMTP_PORT=25
# No authentication needed for EUSD mail server
# SMTP_USER=
# SMTP_PASSWORD=
EMAIL_FROM=automation@eusd.org
EMAIL_TO=server_engineer@eusd.org